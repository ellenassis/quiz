import styled from "styled-components";

export const Container = styled.div`
	text-align: center;
	max-width: 600px;

	h2, p {
		margin-bottom: 1rem;
	}

	.msg {
		margin-top: 3rem;
		color: var(--secondary);
	}

	img {
		margin-top: 4rem;
	}
`