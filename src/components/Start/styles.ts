import styled from "styled-components";

export const Container = styled.div`
	text-align: center;
	max-width: 500px;

	h2, p {
		margin-bottom: 1rem;
	}

	p {
		color: var(--primary);
	}

	img {
		margin-top: 2rem;
	}
`