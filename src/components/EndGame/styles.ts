import styled from "styled-components";

export const Container = styled.div`
	text-align: center;
	max-width: 400px;

	h2, p {
		margin-bottom: 1rem;
	}

	span {
		color: var(--secondary);
		font-weight: bold;
	}

	img {
		margin: 2rem;
	}
`;
